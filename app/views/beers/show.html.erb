<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery.name %>
</p>

<p>
  Average rating:
  <% if @beer.ratings.empty? %>
      beer has not yet been rated!
  <% else %>
   <%= @beer.average_rating %>, from <%= pluralize(@beer.ratings.length, 'rating', 'ratings')%>
  <% end %>
</p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%= link_to 'Back', beers_path %>
